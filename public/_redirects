# Для Cloudflare Pages (если появится): прокси /api → Render.
# При деплое через Worker используется cloudflare-worker.mjs и переменная API_ORIGIN.
/api/*  https://genshincalculator-l6rw.onrender.com/api/:splat  200
